[project]
name = "ai-yield-rebalancer"
version = "0.1.0"
description = "AI-powered DeFi yield rebalancing system"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"
